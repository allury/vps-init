# 贡献指南

感谢你改进 VPS Hardening Toolkit。由于脚本会修改系统和远程登录配置，变更应优先保证可恢复性、兼容性和清晰的失败提示。

## 开发流程

1. 从最新的 `main` 创建分支。
2. 将每次改动限制在一个明确目标内。
3. 为破坏性操作提供确认、校验或回滚路径。
4. 在一次性或可恢复的 VPS 上完成实际测试。
5. 提交 Pull Request，并说明测试环境与结果。

## 必需检查

```bash
bash -n vps.sh
shellcheck --severity=error vps.sh
```

还应至少验证：

- Debian 与 Ubuntu 分支不会互相误触发；
- SSH 配置通过 `sshd -t` 后才重启服务；
- 网络或 SSH 变更失败时保留可恢复路径；
- 重复执行不会产生明显的重复配置；
- 日志和终端输出不包含密钥、令牌或其他敏感信息。

## 代码风格

- 使用 Bash，文件采用 UTF-8 和 LF 换行；
- 变量引用默认加双引号；
- 函数和变量使用有意义的英文名称；
- 用户提示可以使用中文，但错误信息应给出下一步操作；
- 不提交凭据、主机地址、私钥、日志或本地编辑器配置。

安全漏洞请遵循 [SECURITY.md](SECURITY.md)，不要在公开 Issue 中披露。
